# Git e GitHub
## Estrutura
`app/`, `docs/`, `tests/`, `static/`, `templates/`, `.gitignore`, `README.md`, `requirements.txt`, `app.py`.

## Commits sugeridos
`docs: adiciona engenharia de requisitos`
`docs: adiciona visão do produto`
`docs: adiciona SRS`
`docs: adiciona modelo de dados`
`feat: adiciona upload`
`feat: adiciona extrator PDF`
`test: adiciona testes do PDF`
`feat: adiciona exportação CSV`

## Não versionar
`venv/`, arquivos enviados, segredos, banco local, temporários e credenciais.
